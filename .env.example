# The Kopala ICR 2026 backend API — same one the public site (kopalaicr)
# and this dashboard both talk to. 8004 matches kopalaicr-api's local dev
# port (see its README/docker-compose.yml).
VITE_API_BASE_URL=http://localhost:8004
